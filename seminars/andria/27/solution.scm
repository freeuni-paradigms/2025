;1
(define (reverse_ lst))

;2
(define (sorted? lst))

;3
(define (max_ lst))

(define (filter_ func lst))

(define (max_divisible3 lst))

;4
(define (range k))

(define (pow a n))

(define (generate_powers a k))